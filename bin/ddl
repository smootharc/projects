#!/bin/sh

if numfmt -- "$1" > /dev/null 2>&1; then

    find ~/Downloads -ignore_readdir_race -type f -cmin +"$1" -delete

else

    echo "No or non-numeric time specified.  No files deleted."			

fi

find ~/Downloads -ignore_readdir_race -mindepth 1 -type d -empty -delete
