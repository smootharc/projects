#!/bin/sh

if numfmt -- "$1" > /dev/null 2>&1; then

    find ~/Downloads -type f -cmin +"$1" -delete

else

    echo "No or non-numeric time specified.  No files deleted."			

fi

find ~/Downloads -mindepth 1 -type d -empty -delete
