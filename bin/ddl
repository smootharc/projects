#!/bin/bash

t="$1"

rmempty() {

    find ~/Downloads -mindepth 1 -empty -delete 2> /dev/null

}

help() {

    echo -e "\nUsage: $(basename $0) M\n\nM must be an integer greater than or equal to zero.  No files deleted.\n"			
    exit "$1"
    
}

trap rmempty EXIT

[[ $# -eq 0 ]] && help 1

[[ $t == "-h" ]] && help 0

if [[ $t =~ ^[[:digit:]]+$ ]]; then

    find ~/Downloads -mindepth 1 -cmin +"$t" -delete 2> /dev/null

else

    help 1

fi

