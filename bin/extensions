#!/bin/sh

if test "$#" -ne 1; then

    printf "\nUsage: %s filetype\n\n" $(basename $0)
    exit 1

fi

grep "$1" /etc/mime.types | tr -s " \t" "|" | cut --complement -s -d "|" -f 1 | tr "\n" "|" | sed 's/|$//'
