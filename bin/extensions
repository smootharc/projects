#!/usr/bin/env bash

if test "$#" -ne 1; then

    printf "\nUsage: %s filetype\n\n" $(basename $0)
    exit 1

fi

grep "$1" /etc/mime.types | tr -s "\t" | cut -s -f 2 | tr " " "\n" | tr "\n" "|" | sed 's/|$//'
