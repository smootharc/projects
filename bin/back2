#!/bin/sh

if [ -z "$1" ]; then
    echo "Usage: $(basename $0) sshservername."
    exit 1  
fi

cd && rsync -avr --delete --timeout=60 -f "exclude xfce4-power-manager.xml" -f "exclude .debris/" --files-from=.back2.lst --log-file=/tmp/back2.log . "$1":~/
