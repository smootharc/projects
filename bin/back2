#!/bin/sh

if [ -z "$1" ]; then
    echo "No remote machine specified."
    exit 1  
fi

cd && chronic rsync -avr --delete -f "exclude xfce4-power-manager.xml" --files-from=.back2.lst --log-file=/tmp/back2.log . "$1":~/
