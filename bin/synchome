#!/bin/sh

if [ -z "$1" ]; then
    echo "Usage: $(basename $0) sshservername."
    exit 1  
fi

cd && rsync -arP --delete --timeout=60 --exclude 'watch_later/*' --delete-excluded --files-from=.config/rsync/from-home ~ "$1"::paul
