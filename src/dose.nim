import std/os
import std/parseopt
import std/strutils
import db_connector/db_sqlite
import std/times
import std/options
import noise

proc select() =
  discard

proc insert() =
  discard

proc update() =
  discard

proc delete() =
  discard

proc count() =
  discard

proc main() =
  discard

main()
